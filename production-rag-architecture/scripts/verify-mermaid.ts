import { readFile, readdir } from "node:fs/promises"; import { join } from "node:path"; import { fileURLToPath } from "node:url";
const directory=fileURLToPath(new URL("../diagrams",import.meta.url)); const files=(await readdir(directory)).filter(x=>x.endsWith(".mmd")); const errors:string[]=[]; for(const file of files){const source=await readFile(join(directory,file),"utf8");if(!/^(flowchart|sequenceDiagram|stateDiagram-v2)\b/.test(source.trim()))errors.push(`${file}: declaration`);for(const [a,b] of [["[","]"],["(",")"],["{","}"]] as const)if((source.split(a).length)!==(source.split(b).length))errors.push(`${file}: unbalanced ${a}${b}`)} if(files.length!==6)errors.push(`Expected 6 diagrams, found ${files.length}`); if(errors.length){console.error(errors.join("\n"));process.exitCode=1}else console.log("Mermaid verification passed (6 diagrams).");
